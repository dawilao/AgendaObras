/*
 * Modelo de arquivo de configuração de Email para AgendaObras
 * 
 * INSTRUÇÕES DE USO:
 * 1. Copie este arquivo e renomeie para: email_config.json
 * 2. Preencha os valores abaixo com suas credenciais reais de email SMTP
 * 3. Salve o arquivo
 * 4. Configure o sistema para usar este arquivo de configuração
 * 
 * IMPORTANTE: 
 * - Não compartilhe este arquivo com suas senhas!
 * - Adicione email_config.json ao .gitignore para evitar commit acidental
 * 
 * PARÂMETROS:
 * - smtp_server: Servidor SMTP (ex: smtp.gmail.com, smtp.outlook.com, smtp.office365.com)
 * - smtp_port: Porta SMTP (geralmente 587 para TLS ou 465 para SSL)
 * - smtp_user: Usuário SMTP (geralmente seu endereço de email completo)
 * - smtp_password: Senha SMTP ou senha de aplicativo
 *   Para Gmail: use uma "Senha de app" gerada em https://myaccount.google.com/apppasswords
 *   Para outros provedores: consulte a documentação do seu provedor de email
 * - email_remetente: Email que aparecerá como remetente das notificações
 * - email_destinatarios: 
 * - usar_tls: Usar TLS para conexão segura (true ou false)
 */

{
    "smtp_server": "smtp.gmail.com",
    "smtp_port": 587,
    "smtp_user": "seu-email@exemplo.com",
    "smtp_password": "sua-senha-ou-senha-de-app",
    "email_remetente": "seu-email@exemplo.com",
    "email_destinatarios": [
        "email-do-destinatario@exemplo.com",
    ],
    "usar_tls": true
} 